---
import type { Article } from '../api/article-api';

interface Props {
  article: Article;
}

const { article } = Astro.props;
---

<article>
  <header>
    <h1>{article.title}</h1>
    <p>{article.date}</p>
  </header>
  <p>{article.body}</p>
  <div><a href={`/`}>トップに戻る</a></div>
</article>
